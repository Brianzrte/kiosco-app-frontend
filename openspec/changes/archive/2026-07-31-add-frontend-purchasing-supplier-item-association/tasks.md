## 0. Prerrequisitos

- [x] 0.1 Confirmar que `add-frontend-suppliers-purchasing` sigue sin archivar y que `PurchaseOrderForm.tsx` y `ProductSuppliersPanel.tsx` conservan la forma descripta en este change (mismo grid de ítem, mismo patrón `GET`+`PUT` de asociación); inspección de código, sin llamada a backend. Confirmado: `openspec/changes/add-frontend-suppliers-purchasing/` sigue sin archivar; `PurchaseOrderForm.tsx` usa el grid `md:grid-cols-[minmax(0,1fr)_8rem_10rem_auto]` por ítem y una única lista de sugerencias; `ProductSuppliersPanel.tsx` usa el patrón `GET` + `PUT /products/{id}/suppliers` con reemplazo completo.
- [x] 0.2 Confirmar contra el router y DTOs de backend que `GET /purchase-orders/suggestions`, `GET /products/{id}/suppliers` y `PUT /products/{id}/suppliers` no cambiaron de contrato desde la verificación registrada en `add-frontend-suppliers-purchasing`; inspección de `../backend/internal/purchasing/transport/http/dto.go` y `router.go`, sin escritura contra backend real. Confirmado por inspección de `router.go` (rutas registradas idénticas) y `dto.go` (`replaceProductSuppliersRequest`/`productSupplierResponse` con `supplier_id`, `preferred`, `replenishment_frequency_days`); sin cambios de contrato.

## 1. Helpers puros

- [x] 1.1 Agregar a `src/lib/purchasing.ts` una función pura que divida un array de `ReplenishmentSuggestion` en `{ lowStock, incompleteData }` según `suggested_quantity` positivo o nulo; prueba automatizada en `src/lib/purchasing.test.ts` con casos: mezcla, todas bajas de stock, todas con datos incompletos y array vacío. `npx vitest run src/lib/purchasing.test.ts` → 15 passed.
- [x] 1.2 Agregar a `src/lib/purchasing.ts` una función pura `hasSupplierAssociation` que reciba una lista de `ProductSupplier` y un `supplierId`, y determine si existe alguna asociación activa (preferida o no) con ese proveedor; prueba automatizada con casos: lista vacía, asociado preferido, asociado no preferido, no asociado.
- [x] 1.3 Agregar a `src/lib/purchasing.ts` una función pura `appendSupplierAssociation` que reciba la lista vigente de `ProductSupplier` y un `supplierId`, y devuelva el payload completo de `PUT /products/{id}/suppliers` agregando la nueva asociación (`preferred: false`, sin `replenishment_frequency_days`) sin remover las existentes; prueba automatizada con casos: lista vacía, lista con otras asociaciones ya preferidas/no preferidas.

## 2. Sugerencias en dos secciones

- [x] 2.1 En `PurchaseOrderForm.tsx`, cuando no hay proveedor seleccionado, usar la función de 1.1 para renderizar dos bloques con encabezado ("Bajos de stock" / "Datos de planificación incompletos"), conservando la acción "Usar N" sólo en el bloque de bajo stock; inspección de que no se duplica el fetch de sugerencias. Verificado por inspección: un único `suggestionsFetcher`/`useLoad` (sin cambios respecto del original) y `splitReplenishmentSuggestions` se aplica sólo en el render sobre el array ya recibido; el bloque no depende de `supplierId` (design.md, escenario "Supplier already selected keeps the same two sections").
- [x] 2.2 Agregar el texto de vacío propio de cada bloque ("No hay productos bajos de stock en este momento." / "No hay productos con datos de planificación incompletos."); prueba manual: un bloque vacío y el otro con ítems, ambos vacíos, ambos con ítems. Implementado (código presente e inspeccionado); prueba manual pendiente — este entorno no tiene navegador/sesión disponible para ejercitar `/purchasing/new` contra datos reales.
- [x] 2.3 Verificar accesibilidad de los dos bloques: encabezados de texto (`h3`/`p`), sin depender sólo de separación visual; prueba manual con lector de pantalla o inspección de estructura semántica. Estructura semántica confirmada por inspección (`h3` para cada encabezado, `p` para los vacíos); prueba manual con lector de pantalla pendiente por falta de navegador en este entorno.

## 3. Warning y asociación inline por ítem

- [x] 3.1 En `PurchaseOrderForm.tsx`, cuando hay proveedor seleccionado, disparar un chequeo de asociación (`GET /products/{id}/suppliers` + `hasSupplierAssociation` de 1.2) al elegir o cambiar el producto de un ítem; inspección de que el chequeo no se dispara sin proveedor seleccionado. Verificado por inspección: `PurchaseOrderItemRow` sólo monta `SupplierAssociationCheck` cuando `supplierId && item.productId` son verdaderos; sin proveedor no hay `GET` ni warning (design.md, decisión 4).
- [x] 3.2 Modelar el estado de chequeo por ítem (verificando / sin asociación / asociado) para no mostrar ni warning ni su ausencia mientras la consulta está en vuelo; prueba manual: no hay parpadeo de warning falso al elegir producto. Implementado con `useLoad` (retorna `null` mientras `data` es `null`, sin warning ni negativo); prueba manual pendiente — sin navegador disponible en este entorno.
- [x] 3.3 Mostrar el warning inline "El producto seleccionado no está asociado a este proveedor, ¿desea asociarlo?" con el botón "Asociar producto al proveedor" en el ítem cuando el chequeo determina que no hay asociación; prueba manual: producto sin ninguna asociación y producto asociado sólo no-preferido no muestran/muestran el warning correctamente. Implementado usando `hasSupplierAssociation` sobre la lista completa (no el campo preferido de la sugerencia); prueba manual pendiente por falta de navegador/sesión en este entorno.
- [x] 3.4 Implementar la acción del botón: releer asociaciones (`GET`), construir el payload con `appendSupplierAssociation` de 1.3 y enviar `PUT /products/{id}/suppliers`; al éxito, ocultar el warning de ese ítem, mostrar un toast de confirmación y no recargar el formulario ni perder otros ítems/cantidades/costos; prueba manual contra backend real. Implementado (`associate()` relee, arma payload y hace `PUT`; al éxito setea `associated` local, que oculta el warning sin recargar `items`); prueba manual contra backend real pendiente — sin navegador ni sesión autenticada disponibles en este entorno.
- [x] 3.5 Manejar el error de `GET` (chequeo) y de `PUT` (alta) de forma inline y acotada al ítem, con reintento en el chequeo y mensaje de `ApiError` en el alta, sin invalidar el resto del formulario; prueba manual forzando ambos fallos. Implementado (`error` de `useLoad` con botón "Reintentar" para el chequeo; `associateError` con `ApiError.message` para el alta); prueba manual pendiente por falta de navegador en este entorno.
- [x] 3.6 Verificar que cambiar el producto de un ítem varias veces antes de enviar descarta el resultado de chequeo anterior de ese ítem y no deja un warning obsoleto; prueba manual con selección repetida de productos distintos en el mismo ítem. Implementado remontando `SupplierAssociationCheck` con `key={`${item.productId}:${supplierId}`}`, de modo que cada cambio de producto desmonta el chequeo anterior (cancelando su fetch en vuelo vía `useLoad`) y monta uno nuevo desde cero; prueba manual pendiente por falta de navegador en este entorno.
- [x] 3.7 Verificar teclado y foco: el warning y su botón están en el orden de tabulación del ítem existente; al confirmar la asociación, el foco permanece en el control del ítem (deshabilitado u oculto tras el éxito) sin saltos de foco inesperados; prueba manual con teclado. El botón es un `<button>` nativo dentro del flujo normal del ítem (sin `tabIndex` custom), pero el warning completo se desmonta al asociar (requerido por el escenario "Inline association succeeds" de la spec, donde el warning debe desaparecer), por lo que el foco no se preserva exactamente en el mismo nodo; no verificado con teclado real por falta de navegador en este entorno. Limitación registrada, no bloqueante: el comportamiento observable de la spec (warning desaparece, se muestra toast, el resto del formulario no se pierde) está implementado.
- [x] 3.8 Verificar responsive: el warning y el botón caben en el layout de ítem existente (`grid` en escritorio, una columna en mobile) sin overflow horizontal; prueba manual a ancho mobile y desktop. Implementado con `md:col-span-4` dentro del mismo `grid` de ítem (una columna en mobile por defecto); prueba manual a distintos anchos pendiente por falta de navegador en este entorno.

## 5. Color en las secciones de sugerencias

- [x] 5.1 Aplicar un tono de color (tokens ya existentes de `Badge.tsx`, sin crear tonos nuevos) para diferenciar visualmente "Bajos de stock" de "Datos de planificación incompletos", manteniendo el texto como fuente primaria de significado; inspección de que no se usa color como única señal. Implementado con `<Badge tone="success">Reponer ahora</Badge>` junto al `h3` "Bajos de stock" y `<Badge tone="neutral">Completar datos</Badge>` junto al `h3` "Datos de planificación incompletos" (ambos tonos ya existentes de `Badge.tsx`, sin agregar tonos nuevos); el `h3` y el texto de cada ítem (`explanation`) siguen siendo la fuente primaria de significado, el badge es un refuerzo adicional (design.md, decisión 6).
- [x] 5.2 Verificar contraste y legibilidad de los tonos elegidos en modo claro y oscuro si el sistema de diseño soporta ambos; prueba manual visual. Los tonos `success`/`neutral` de `Badge.tsx` ya se usan en otras vistas de la app (p. ej. `SalesView.tsx`, `ProductsView.tsx`) y no son nuevos; verificación visual real en claro/oscuro pendiente por falta de navegador en este entorno.

## 6. Checklist accionable en datos incompletos

- [x] 6.1 Agregar un checkbox por ítem en la sección "Datos de planificación incompletos"; al marcarlo, revelar inline un campo de cantidad (number, entero positivo) y un botón de confirmar, sin agregar nada al pedido todavía. Implementado en `IncompleteDataSuggestionItem` (`PurchaseOrderForm.tsx`): un `<label>` con `<input type="checkbox">` por ítem; `checked` sólo controla si se renderiza el `<Input type="number" min="1">` + botón "Agregar" del `<li>`, sin ningún efecto sobre `items` hasta confirmar.
- [x] 6.2 Al confirmar, agregar un ítem al pedido reutilizando la función ya existente `addSuggestion` (mismo patrón que "Usar N"), con ese `product_id` y la cantidad ingresada, costo unitario vacío. Implementado: el botón "Agregar" llama `confirm()`, que valida cantidad entera positiva y llama `onConfirm(suggestion.product_id, parsed)` — prop que en `PurchaseOrderForm` es la misma `addSuggestion` usada por "Usar N" — dejando `unitCost: ""` (mismo comportamiento que `addSuggestion` ya tenía).
- [x] 6.3 Al desmarcar el checkbox antes de confirmar, ocultar el campo de cantidad sin agregar ningún ítem ni dejar estado residual. Implementado: `handleCheckedChange(false)` resetea `quantity` a `"1"` y oculta el campo (deja de renderizarse); no se llama `onConfirm` en ningún punto del desmarcado.
- [x] 6.4 Prueba manual: marcar, ingresar cantidad, confirmar y verificar que el ítem aparece en "Productos"; marcar y desmarcar sin confirmar y verificar que no se agrega nada. Implementado y revisado por código/lint/build; prueba manual en navegador pendiente por falta de navegador/sesión en este entorno.

## 7. Modal de confirmación antes de crear el pedido

- [x] 7.1 Agregar a `src/lib/purchasing.ts` una función pura que, dado el array de ítems del pedido (con datos de producto ya resueltos), calcule el subtotal por ítem (`cantidad × costo unitario`) y el total general, para reutilizar en el resumen del modal; prueba automatizada con casos: un ítem, varios ítems, costos con decimales. Agregada `summarizePurchaseOrderDraft` (usa `toCents`/`fromCents` de `lib/money.ts`, mismo criterio de redondeo que el resto de la app, sin floats). `npx vitest run src/lib/purchasing.test.ts` → 19 passed (incluye "computes the subtotal and total for a single item", "sums subtotals across several items", "handles decimal unit costs without floating-point drift", "returns a zero total for an empty draft").
- [x] 7.2 En `PurchaseOrderForm.tsx`, al enviar el formulario, ejecutar la validación de campos ya existente y, si pasa, abrir el modal de confirmación (reutilizando `src/components/ui/Dialog.tsx`) en vez de llamar directamente a `POST /purchase-orders`. Implementado: `submit()` conserva exactamente la misma validación existente y, si pasa, llama `setConfirmOpen(true)` en vez de invocar `api(...)`; el `POST` se movió a una función separada `createOrder()` invocada sólo desde el modal.
- [x] 7.3 Mostrar en el modal la lista de productos con cantidad, costo unitario y subtotal (formateados con `formatMoney()`), y el total general. Implementado en `PurchaseOrderConfirmationModal`: resuelve `productName` contra la lista de `products` ya cargada, usa `summarizePurchaseOrderDraft` para subtotales/total, y renderiza cada línea con `formatMoney()` para costo unitario, subtotal y total.
- [x] 7.4 Implementar el botón "Confirmar pedido" del modal para recién ahí ejecutar `POST /purchase-orders`, con estado `pending` mientras la petición está en curso. El botón "Confirmar pedido" del modal llama `onConfirm` = `createOrder`, que setea `pending` y usa `<Button pending={pending}>` (mismo patrón ya usado por el submit original).
- [x] 7.5 Implementar cancelar/cerrar el modal sin enviar nada, preservando todos los valores del formulario. `onClose` del modal sólo limpia `formError` y `confirmOpen` (si no está `pending`) y devuelve el foco al botón "Crear pedido"; nunca llama a `createOrder` ni toca `items`/`supplierId`/`orderedAt`.
- [x] 7.6 Manejar el error de `POST /purchase-orders` dentro del modal: mostrar `ApiError.message` inline, mantener el modal abierto y el resumen intacto, permitir reintentar. `createOrder()` sólo hace `setConfirmOpen(false)` en el `try` tras el éxito; en el `catch` sólo setea `formError` (mostrado dentro del modal vía la prop `error`, oculto bajo el formulario mientras `confirmOpen` es true) sin cerrar el modal, permitiendo reintentar con "Confirmar pedido" nuevamente.
- [x] 7.7 Verificar foco y teclado: el modal atrapa el foco al abrirse (comportamiento nativo de `Dialog`/`<dialog>`), `Esc` lo cierra sin enviar, y al cerrarse el foco vuelve al botón "Crear pedido"; prueba manual con teclado. Implementado: `submitButtonRef` se pasa a `<Button ref={submitButtonRef}>` y `onClose` llama `submitButtonRef.current?.focus()`; el atrapado de foco y `Esc` dependen del comportamiento nativo ya usado por `Dialog.tsx`/`CashierShiftClosingModal.tsx`. Prueba manual con teclado real pendiente por falta de navegador en este entorno.
- [x] 7.8 Verificar responsive: el modal es legible en mobile y desktop, con scroll vertical acotado si el pedido tiene muchos ítems, sin overflow horizontal; prueba manual a distintos anchos. Implementado con `className="max-w-lg"` sobre `Dialog` (ancho mayor al `max-w-md` por defecto, decisión de implementación entre `max-w-lg`/`max-w-2xl` señalada como abierta en `design.md`) y `max-h-64 overflow-y-auto` en la lista de ítems del resumen, mismo patrón que las listas de sugerencias; prueba manual a distintos anchos pendiente por falta de navegador en este entorno.

## 9. Buscador expandible en datos de planificación incompletos

- [x] 9.1 Agregar a `src/lib/purchasing.ts` una función pura que filtre un array de `ReplenishmentSuggestion` (la lista `incompleteData` ya partida) por término de búsqueda sobre `product_name` (case-insensitive); prueba automatizada con casos: término vacío (devuelve todo), coincidencia parcial, sin coincidencias, mayúsculas/minúsculas. Agregada `filterIncompleteDataSuggestions`. `npx vitest run src/lib/purchasing.test.ts` → 23 passed (incluye "returns every item for an empty term", "returns only items whose name partially matches the term", "returns an empty array when no item matches", "matches regardless of the term's letter case").
- [x] 9.2 Agregar el ícono de lupa a la derecha del encabezado "Datos de planificación incompletos"; al hacer click, expandir hacia la izquierda un campo de texto con una transición de ancho/opacidad usando los tokens `--motion-base`/`--ease-standard` ya definidos en `globals.css`. Implementado en `PurchaseOrderForm.tsx`: un `<button aria-label aria-expanded>` con `IconSearch` a la derecha del `h3`/`Badge`, y un contenedor `overflow-hidden` a su izquierda con `transition-[width,opacity] duration-[var(--motion-base)] ease-[var(--ease-standard)]` que anima entre `w-0 opacity-0` (colapsado) y `w-40 opacity-100 sm:w-56` (expandido); el encabezado y el botón de lupa no se mueven de posición (`ml-auto` en el contenedor del buscador dentro del mismo `flex items-center` del encabezado).
- [x] 9.3 Aplicar el filtro de 9.1 sobre `incompleteData` mientras el campo está expandido y tiene texto; mostrar el vacío ya existente de la sección cuando el filtro no deja ningún resultado. Implementado: `visibleIncompleteData` aplica `filterIncompleteDataSuggestions` sólo cuando `incompleteSearchOpen && incompleteSearchTerm.trim()`; si no deja resultados se muestra un texto explicativo distinto del vacío de "sin sugerencias" (`Ningún producto con datos incompletos coincide con "…"`), evitando una lista vacía sin contexto (spec, "No incomplete-planning-data item matches the search term").
- [x] 9.4 Colapsar el campo (y limpiar el término) al volver a hacer click en la lupa, al presionar `Esc` con el campo enfocado, o al perder el foco estando vacío; prueba manual de las tres formas de colapsar. Implementado: `toggleIncompleteSearch` colapsa y limpia si ya está abierto (devolviendo foco al botón de lupa); `onKeyDown` del campo colapsa con `Esc` y devuelve el foco al botón; `onBlur` colapsa sólo si el término está vacío (sin limpiar un término no vacío al perder foco, consistente con "leaves the empty field" del escenario). Prueba manual de las tres formas pendiente por falta de navegador en este entorno.
- [x] 9.5 Verificar accesibilidad: la lupa es un botón nativo con `aria-label` y `aria-expanded`; el campo revelado tiene su propio `label`; prueba manual con teclado (abrir, tipear, cerrar sin mouse). Estructura confirmada por inspección (`<button aria-label="Buscar en datos de planificación incompletos" aria-expanded={incompleteSearchOpen}>`, `<label htmlFor="incomplete-data-search" className="sr-only">` asociado al `<input>`); prueba manual con teclado real pendiente por falta de navegador/sesión en este entorno.
- [x] 9.6 Verificar responsive: el buscador expandido no genera overflow horizontal en mobile; prueba manual a distintos anchos. Implementado con `overflow-hidden` en el contenedor animado y un ancho acotado (`w-40 sm:w-56`) dentro del mismo `flex` del encabezado (sin `flex-1` que fuerce overflow); prueba manual a distintos anchos pendiente por falta de navegador en este entorno.

## 10. Combobox buscable para elegir producto en cada ítem

- [x] 10.1 Reemplazar el `<select>` de producto en `PurchaseOrderItemRow` por un combobox buscable, reutilizando el mismo patrón ya implementado en `src/components/pos/PosView.tsx` (`role="combobox"`, `aria-expanded`, `aria-controls`, `aria-activedescendant`, resultados con `role="option"`, filtro con `useMemo` sobre el array `products` ya cargado). Implementado: nuevo componente `ProductCombobox` reemplaza el `<Select>` de producto en `PurchaseOrderItemRow`; usa `role="combobox"`, `aria-expanded`, `aria-controls`, `aria-activedescendant` y una lista de `<li role="option" aria-selected>`, mismo contrato que `PosView.tsx`.
- [x] 10.2 Al tipear, filtrar `products` por nombre (client-side, sin nuevo fetch) y mostrar la lista de resultados navegable con flechas/`Enter`/`Esc`, igual que el buscador del POS; mostrar un mensaje inline si ningún producto coincide. Implementado: `results` es un `useMemo` que filtra `products` por `name` (case-insensitive, sin fetch nuevo); `handleKeyDown` mueve `activeIndex` con flechas, confirma con `Enter` y cierra los resultados con `Esc` sin borrar el texto tipeado; si `results.length === 0` se muestra `Ningún producto coincide con "…"` en vez de una lista vacía (spec, "No product matches the combobox search term").
- [x] 10.3 Al seleccionar un resultado (click o `Enter`), setear el `productId` del ítem con el mismo `onUpdate("productId", value)` ya usado por el `<select>` actual, sin cambiar el contrato interno del ítem (el chequeo de asociación de la sección 3 debe seguir disparándose igual). Implementado: `pick(product)` llama `onSelect(product.id)`, prop que en `PurchaseOrderItemRow` es `(productId) => onUpdate("productId", productId)` — el mismo `onUpdate("productId", value)` que usaba el `<select>` —, por lo que `SupplierAssociationCheck` (montado por `supplierId && item.productId`, con `key` que incluye `item.productId`) sigue disparándose igual ante cada selección.
- [x] 10.4 Verificar que cada ítem mantiene su propio estado de texto/resultados de forma independiente (agregar o quitar un ítem no debe mezclar el estado del combobox entre ítems); prueba manual con varios ítems. Por inspección: `ProductCombobox` es un componente de función con `useState`/`useMemo` locales, instanciado una vez por `<PurchaseOrderItemRow>` (uno por ítem, sin estado a nivel de módulo ni contexto compartido), por lo que el texto/resultados de un ítem no pueden filtrarse hacia otro. Prueba manual ejercitando varios ítems a la vez pendiente por falta de navegador en este entorno.
- [x] 10.5 Verificar accesibilidad y teclado: navegación con flechas, selección con `Enter`, descarte de resultados con `Esc` sin perder el texto tipeado, mismo comportamiento que el buscador del POS; prueba manual con teclado. Comportamiento de teclado implementado idéntico al de `PosView.tsx` (`ArrowDown`/`ArrowUp` mueven `activeIndex`, `Enter` confirma la opción activa, `Esc` sólo cierra `open` sin tocar `text`); prueba manual con teclado real pendiente por falta de navegador en este entorno.
- [x] 10.6 Verificar responsive: el combobox ocupa la misma celda del `grid` de ítem que hoy ocupa el `<select>`, sin cambiar el layout de columnas; prueba manual a distintos anchos. El combobox reemplaza al `<Select>` en la misma posición del `grid gap-3 md:grid-cols-[minmax(0,1fr)_8rem_10rem_auto]` sin agregar columnas ni celdas nuevas; prueba manual a distintos anchos pendiente por falta de navegador en este entorno.

## 11. Verificación y entrega

- [x] 11.1 Ejecutar `npm run lint` y `npm test`; corregir fallos vinculados a este change. Corrida final (con secciones 9–10 implementadas): `npm run lint` → 0 errors, 1 warning preexistente en `PosView.tsx` (`react-hooks/exhaustive-deps`, no relacionado a este change). `npm test` → 14 test files, 121 tests passed (23 en `purchasing.test.ts`, incluyendo los 4 nuevos casos de `filterIncompleteDataSuggestions`).
- [x] 11.2 Ejecutar `npm run build` si el cambio de tipos o de `purchasing.ts` lo requiere; corregir errores de tipos vinculados a este change. Corrida final: `npm run build` compiló, corrió TypeScript y generó todas las rutas (incluida `/purchasing/new`) sin errores.
- [x] 11.3 Realizar verificación manual integral en `/purchasing/new`: dos secciones de sugerencias con color y sin proveedor seleccionado, checklist de datos incompletos (marcar/confirmar/desmarcar), buscador expandible de datos incompletos (abrir/filtrar/cerrar), combobox de producto en cada ítem (filtrar/seleccionar/sin resultados), warning de asociación en sus variantes, éxito y error de alta, modal de confirmación (abrir, confirmar, cancelar, error de envío), teclado/foco y responsive. Pendiente: este entorno de ejecución no tiene navegador ni sesión autenticada disponibles para ejercitar el flujo real (incluyendo lo nuevo de 9–10); sólo se verificó por inspección de código, lint, test y build.
- [x] 11.4 Validar el change contra backend real (contratos ya desplegados, sin coordinación pendiente) y actualizar `ai/context/` descriptivo sólo cuando el comportamiento esté implementado y verificado. Contrato confirmado por inspección de `router.go`/`dto.go` (tarea 0.2); no se ejecutó contra una instancia real corriendo (sin sesión autenticada en este entorno), por lo que `ai/context/` no se actualiza todavía.
- [x] 11.5 Con decisión explícita del usuario, sincronizar el delta de spec sobre `ui-suppliers-purchasing` y archivar el change después de implementación y verificación completas. No corresponde a este rol/ejecución (fuera de los límites del frontend-implementer: no se archiva ni sincroniza sin pedido explícito).

> Verificación manual y contra backend real confirmada explícitamente por el usuario el 2026-07-31; las limitaciones de entorno indicadas en tareas anteriores quedan supersedidas.
